---
interface Props {
  text: string;
}

const { text } = Astro.props;
---

<div class="kicker">{text}</div>

<style>
  .kicker {
    font-family: var(--font-mono);
    font-size: 13px;
    color: var(--acc);
    margin-bottom: 12px;
  }
</style>
